import React from 'react';
import ProfileImage from '../../components/auth/ProfileImage/index';

const CatRegisterContainer = () => {
  return <ProfileImage />;
};

export default CatRegisterContainer;
